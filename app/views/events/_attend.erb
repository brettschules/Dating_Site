<%= form_tag( event_users_path,method: "POST") do %>
<%=hidden_field_tag(:user_id, current_user.id) %>
<%=hidden_field_tag(:event_id, @event.id) %>
<%= submit_tag("Attend", class: "btn btn-danger btn-lg") %>
<% end %>
